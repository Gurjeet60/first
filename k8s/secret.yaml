apiVersion: v1
kind: Secret

metadata:
  name: laravel-secret
  namespace: laravel

type: Opaque

stringData:
  APP_KEY: "${APP_KEY}"
  DB_PASSWORD: "${DB_PASSWORD}"
  MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD}"

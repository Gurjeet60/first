apiVersion: v1
kind: Service
metadata:
  name: laravel-service
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: laravel-app
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 80

apiVersion: v1
kind: Service

metadata:
  name: mysql
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: mysql

  ports:
  - name: mysql
    protocol: TCP
    port: {{ .Values.database.port }}
    targetPort: {{ .Values.database.port }}
  type: ClusterIP

